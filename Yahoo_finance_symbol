import pandas as pd
import datetime
import time
import numpy as np

from yahooquery import Ticker
pd.set_option('display.max_rows', None)
pd.set_option('display.max_columns', None)

symbol = 'RACE' 
stock = Ticker(symbol)
stock.balance_sheet()

print("=== INCOME STATEMENT ===")
print(stock.income_statement().columns.tolist())

print("\n=== BALANCE SHEET ===")
print(stock.balance_sheet().columns.tolist())

print("\n=== CASH FLOW ===")
print(stock.cash_flow().columns.tolist())
